"use client";

import ExchangeHistoryPage from "@/components/features/exchange-history";

export default function OrdersPage() {
   // RouteGuard가 전역적으로 라우트 보호를 처리하므로 여기서는 단순히 컴포넌트만 렌더링
   return <ExchangeHistoryPage />;
}
